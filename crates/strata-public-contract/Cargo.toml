[package]
name = "strata-public-contract"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Deliberately narrow, versioned public product contract for Strata clients."
readme = "README.md"
keywords = ["solana", "trading", "contract", "strata"]
categories = ["api-bindings"]
publish = ["crates-io"]

[features]
fixtures = []

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
