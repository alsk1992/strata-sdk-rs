[package]
name = "strata-public-contract"
version = "0.2.5"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Shared Rust types for Strata markets and Sonar quotes."
readme = "README.md"
keywords = ["solana", "trading", "contract", "strata"]
categories = ["api-bindings"]
publish = ["crates-io"]

[features]
fixtures = []

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
